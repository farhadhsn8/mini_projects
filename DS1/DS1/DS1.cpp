// DS1.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <iostream>

using namespace std;

int main()
{
	int a[10];
	int s = 0;
	for (int i = 0; i < 10; i++)
		cin >> a[i];
	int x;
	cout << endl << "xra v : ";
	cin >> x;
	for (int i = 0; i <= 10; i++) {
		for (int j = 0; j <= 10; j++) {
			s += a[j];
			if (s > x) 
		}
	}
	cout << j-i;
    return 0;
}

